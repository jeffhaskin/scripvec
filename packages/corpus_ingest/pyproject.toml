[project]
name = "scripvec-corpus-ingest"
version = "0.0.0"
description = "Normalized verse records emitted from the canonical scripture source under data/raw/."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/scripvec_corpus_ingest"]
