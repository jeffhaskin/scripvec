# reference

Citation parsing and normalization — turns free-form citation strings like `"Alma 32:21"` or `"D&C 88:118"` into canonical structured references, and renders canonical references back to display strings.

Allowed in-repo imports: none — `reference` is a leaf package in the dependency graph per `docs/specs/adrs/003_accepted_mvp_folder_structure.md`.
