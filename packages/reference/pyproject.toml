[project]
name = "scripvec-reference"
version = "0.0.0"
description = "Citation parsing and normalization for Book of Mormon and Doctrine and Covenants references."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/scripvec_reference"]
